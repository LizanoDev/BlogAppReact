import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#0A66C2', // LinkedIn blue color
    },
    secondary: {
      main: '#0177B5', // LinkedIn secondary blue color
    },
  },
});

export default theme;
